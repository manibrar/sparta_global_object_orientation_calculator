var userCapture = function() {
  this.num1 = 0);
  this.num2 = 0);
  this.ops = "");
  this.result = 0;
  this calc function() {
  alert("the result is " + result);
}
}

  var userCapture = () {
  this.num = number 1;
  this.num = number 2;
  this.ops = operation;
  this.result = equals;
  this calc function() {
    alert("the result is " + result);
  }
}


var Calc = new Object () {
  this.add = "+";
  this.minus = "-";
  this.divide = "/";
  this.multiply = 
}



this.num1 = parseFloat(prompt("first number?"));
this.num2 = parseFloat(prompt("second number?"));
this.ops = prompt("which operation?");





var whichCalculator = prompt("Which calculator would you like to use?  (a) for advanced and (b) for basic ");
var whichCalculator = whichCalculator.toLowerCase();
switch (whichCalculator) {
  case "a":
    basicCalculator();
    break;
  default:

}
